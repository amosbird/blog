[access]
	inheritFrom = All-Projects
[access "refs/*"]
	owner = group user/Amos Bird (amosbird)
[access "refs/for/refs/*"]
	read = group user/Amos Bird (amosbird)
	push = group user/Amos Bird (amosbird)
	label-Code-Review = -2..+2 group user/Amos Bird (amosbird)
	label-Verified = -1..+1 group user/Amos Bird (amosbird)
	rebase = group user/Amos Bird (amosbird)
	removeReviewer = group user/Amos Bird (amosbird)
	submit = group user/Amos Bird (amosbird)
[access "refs/heads/*"]
	read = group user/Amos Bird (amosbird)
	create = group user/Amos Bird (amosbird)
	push = +force group user/Amos Bird (amosbird)
	pushMerge = group user/Amos Bird (amosbird)
[access "refs/tags/*"]
	push = +force group user/Amos Bird (amosbird)
	pushTag = group user/Amos Bird (amosbird)
	pushSignedTag = group user/Amos Bird (amosbird)
